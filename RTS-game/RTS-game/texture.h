#pragma once

struct Texture {
	size_t texture_ind;
	size_t move_cnt;
	size_t attack_cnt;
	size_t texture_delay_move;
	size_t texture_delay_attack;
	size_t deltaX;
	size_t deltaY;
	size_t width;
	size_t height;
	size_t texture_width;
	size_t texture_height;
};
