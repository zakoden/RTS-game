//#include "Game_Map.h"

/*

Game_Map::Game_Map() {

}

Game_Map::~Game_Map() {

}


bool Game_Map::is_rect_intersect(const SDL_Rect* rect1, const SDL_Rect* rect2) {
	return (rect1->y + rect1->h >= rect2->y) &&
		   (rect1->x + rect1->w >= rect2->x) &&
		   (rect1->y <= rect2->y + rect2->h) &&
		   (rect1->x <= rect2->x + rect2->w);
}


uint8_t Game_Map::get_block(uint32_t x, uint32_t y) {
	//return blocks[x + y * width];
	return 1;
}

void Game_Map::set_block(uint32_t x, uint32_t y, uint8_t value) {
	//blocks[x + y * width] = value;
}

void Game_Map::block_draw(SDL_Renderer* renderer, uint32_t x, uint32_t y) {
	//SDL_RenderCopy(renderer, tiles, );
}

void Game_Map::draw(SDL_Renderer* renderer) {
	for (uint32_t y = 0; y < height; ++y) {
		for (uint32_t x = 0; x < width; ++x) {
			//uint32_t ind = x + y * width;
			
		}
	}
}
*/
